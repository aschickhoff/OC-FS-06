<%= form_with(model: activity_name) do |form| %>
  <% if activity_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(activity_name.errors.count, "error") %> prohibited this activity_name from being saved:</h2>

      <ul>
        <% activity_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
